{
  "name": "void-corsair",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "VOID CORSAIR — a browser 3D space-pirate game (Three.js).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test:logic": "node --test \"tests/logic/**/*.test.js\"",
    "test:shot": "node tests/screenshot.mjs",
    "test": "npm run test:logic && npm run test:shot"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "playwright": "^1.48.0",
    "vite": "^5.4.0"
  }
}
